"""
Top-level package for standalone MCP tools (mutual funds, etc.).

This directory is intentionally decoupled from the FastAPI backend
so that tools can be run as a separate MCP server if desired.
"""

